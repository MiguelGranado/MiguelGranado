# Profile repo — MiguelGranado

Hero SVGs are generated with `scripts/hero/build_hero.py`.
Publish: copy `README.md`, `dark.svg`, `light.svg` to repo root; snake workflow is `.github/workflows/snake.yml`.

Credentials and projects must match https://miguel.ulamander.com.
GitHub stats/languages are real API values — do not invent counts or farm profile-view counters.
