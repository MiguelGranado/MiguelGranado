# Snake disabled on purpose: contribution calendar / snake removed from README
# so activity is not highlighted. Re-enable only if you want the arifhaxn snake back.
name: Generate Snake Animation

on:
  workflow_dispatch:

jobs:
  generate:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Snake workflow disabled — contributions kept off the README."
